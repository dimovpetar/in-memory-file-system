# in-memory-file-system

**Simple in-memory file system**

Supported commands:
..* pwd
..* cd directory
..* ls directory
..* rm <file1> ... <filen>
..* cat <file1> <file2> ... > <outputfile>
  cat <file1> <file2> ...
  cat > <outputfile>

