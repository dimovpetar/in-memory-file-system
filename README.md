# in-memory-file-system

**Simple in-memory file system**

Supported commands:
* pwd
* cd *directory*
* ls *directory*
* rm *file...*
* cat *file...> outputfile*
  cat <file1> <file2> ...
  cat > <outputfile>

